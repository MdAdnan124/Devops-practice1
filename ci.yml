name: My First CI

on:
  push:
    branches: [ main ]

jobs:
  run-shell-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Hello Script
        run: echo "🎉 Hello from your first CI pipeline!"